## Auto generated providers.tf ##
## Updated on: {{ timestamp }} ##

provider "aws" {
  region = "{{ provider_region }}"

  assume_role {
    role_arn = "{{ aft_admin_role_arn }}"
  }

  default_tags {
    tags = {
      managed_by = "AFT"
    }
  }
}

provider "aws" {
  alias  = "ct_admin_role"
  region = "{{ provider_region }}"

  assume_role {
    role_arn = "arn:aws:iam::${data.aws_ssm_parameter.organization_management_account_id.value}:role/AWSAFTExecution"
  }

  default_tags {
    tags = {
      managed_by = "AFT"
    }
  }
}
